{
  "name": "petal",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @petal/web dev",
    "build": "pnpm --filter @petal/web build",
    "check": "pnpm -r --if-present typecheck && pnpm -r --if-present lint && pnpm -r --if-present test"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
